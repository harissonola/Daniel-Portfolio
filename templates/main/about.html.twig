{% extends 'base.html.twig' %}

{% block title %}À propos | L'Architecte Digital{% endblock %}

{% block body %}
<!-- About Section -->
<section class="pt-40 pb-32 bg-primary">
    <div class="max-w-7xl mx-auto px-6">
        <div class="grid md:grid-cols-2 gap-20 items-center">
            <div class="reveal relative">
                <div class="aspect-square glass-card p-4 overflow-hidden tilt-element">
                    <img src="{{ asset('images/image-1.png') }}" alt="Daniel" class="w-full h-full object-cover rounded-xl filter grayscale group-hover:grayscale-0 transition-all duration-700">
                </div>
                {# Floating Stats #}
                <div class="absolute -bottom-10 -right-10 glass-card p-6 border-accent/40 reveal delay-300">
                    <div class="text-4xl font-black text-accent mb-1">+50</div>
                    <div class="text-[10px] font-bold uppercase tracking-widest">Projets livrés</div>
                </div>
            </div>

            <div class="reveal">
                <span class="text-accent font-black uppercase tracking-widest text-xs">The Visionary</span>
                <h1 class="text-5xl md:text-7xl font-title font-black mt-4 mb-8">JE SUIS <span class="text-accent">DANIEL</span></h1>
                
                <div class="space-y-6 text-lg text-text-dim leading-relaxed">
                    <p>
                        Spécialiste de la <span class="text-white font-bold">digitalisation haute performance</span>, j'accompagne les entrepreneurs ambitieux pour transformer des concepts bruts en écosystèmes digitaux scalables.
                    </p>
                    <p>
                        Mon approche est simple : fusionner un design sensoriel digne de Tesla avec une robustesse technique impénétrable. Chaque ligne de code est pensée pour la vitesse, chaque pixel pour l'impact.
                    </p>
                    <p>
                        Basé au <span class="text-white font-bold">Benin</span>, j'opère à l'international pour des clients exigeant une réactivité totale (Standard 72H).
                    </p>
                </div>

                <div class="mt-12 flex gap-8">
                    <div>
                        <div class="text-2xl font-black text-white">100%</div>
                        <div class="text-[10px] font-bold uppercase tracking-widest text-text-dim">Engagement</div>
                    </div>
                    <div class="w-px h-10 bg-white/10"></div>
                    <div>
                        <div class="text-2xl font-black text-white">Fullstack</div>
                        <div class="text-[10px] font-bold uppercase tracking-widest text-text-dim">Symfony / Flutter</div>
                    </div>
                    <div class="w-px h-10 bg-white/10"></div>
                    <div>
                        <div class="text-2xl font-black text-white">High UI</div>
                        <div class="text-[10px] font-bold uppercase tracking-widest text-text-dim">Focus Design</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}
