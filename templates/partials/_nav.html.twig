<nav class="fixed top-0 w-full z-50 backdrop-blur-lg border-b border-white/5 bg-primary/40">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
        <a href="{{ path('app_home') }}" class="group flex items-center gap-2">
            <div class="w-10 h-10 bg-accent rounded-lg flex items-center justify-center font-black text-xl group-hover:rotate-[360deg] transition-transform duration-700">D</div>
            <span class="font-title font-bold text-xl tracking-tighter">Daniel<span class="text-accent">.</span></span>
        </a>

        <div class="hidden md:flex gap-10 items-center">
            {% set current_route = app.request.attributes.get('_route') %}
            <a href="{{ path('app_services') }}" class="text-sm font-semibold hover:text-accent transition-colors nav-link {{ current_route == 'app_services' ? 'active text-accent' : '' }}">Services</a>
            <a href="{{ path('app_expertise') }}" class="text-sm font-semibold hover:text-accent transition-colors nav-link {{ current_route == 'app_expertise' ? 'active text-accent' : '' }}">Expertise</a>
            <a href="{{ path('app_projects') }}" class="text-sm font-semibold hover:text-accent transition-colors nav-link {{ current_route == 'app_projects' ? 'active text-accent' : '' }}">Projets</a>
            <a href="{{ path('app_about') }}" class="text-sm font-semibold hover:text-accent transition-colors nav-link {{ current_route == 'app_about' ? 'active text-accent' : '' }}">À propos</a>
            <a href="{{ path('app_contact') }}" class="premium-btn !px-6 !py-2 !text-xs nav-link {{ current_route == 'app_contact' ? 'active' : '' }}">ME CONTACTER</a>
        </div>

        <button id="mobile-menu-btn" class="md:hidden text-2xl">
            <i class="fa-solid fa-bars-staggered"></i>
        </button>
    </div>

    {# Mobile Menu Overlay #}
    <div id="mobile-menu" class="fixed inset-0 bg-gradient-to-br from-[#020617]/95 via-[#0f172a]/95 to-blue-900/90 backdrop-blur-md z-[60] flex flex-col opacity-0 pointer-events-none transition-all duration-500 transform translate-y-full min-h-[100dvh]" data-state="closed">
        {# Mobile Menu Header (Logo & Close) #}
        <div class="w-full px-6 py-4 flex justify-between items-center z-10">
            <a href="{{ path('app_home') }}" class="group flex items-center gap-2">
                <div class="w-10 h-10 bg-accent rounded-lg flex items-center justify-center font-black text-xl group-hover:rotate-[360deg] transition-transform duration-700">D</div>
                <span class="font-title font-bold text-xl tracking-tighter text-white">Daniel<span class="text-accent">.</span></span>
            </a>
            <button class="text-4xl text-white/50 hover:text-accent transition-colors" id="close-menu">
                <i class="fa-solid fa-xmark"></i>
            </button>
        </div>
        
        {# Main Links - Centered #}
        <div class="flex-1 flex flex-col items-center justify-center gap-6 sm:gap-8 px-6 text-center">
            <a href="{{ path('app_home') }}" class="text-4xl sm:text-6xl font-title font-black hover:text-accent transition-all duration-300 transform hover:scale-110 nav-link {{ current_route == 'app_home' ? 'active text-accent' : '' }}">ACCUEIL</a>
            <a href="{{ path('app_services') }}" class="text-4xl sm:text-6xl font-title font-black hover:text-accent transition-all duration-300 transform hover:scale-110 nav-link {{ current_route == 'app_services' ? 'active text-accent' : '' }}">SERVICES</a>
            <a href="{{ path('app_expertise') }}" class="text-4xl sm:text-6xl font-title font-black hover:text-accent transition-all duration-300 transform hover:scale-110 nav-link {{ current_route == 'app_expertise' ? 'active text-accent' : '' }}">EXPERTISE</a>
            <a href="{{ path('app_projects') }}" class="text-4xl sm:text-6xl font-title font-black hover:text-accent transition-all duration-300 transform hover:scale-110 nav-link {{ current_route == 'app_projects' ? 'active text-accent' : '' }}">PROJETS</a>
            <a href="{{ path('app_about') }}" class="text-4xl sm:text-6xl font-title font-black hover:text-accent transition-all duration-300 transform hover:scale-110 nav-link {{ current_route == 'app_about' ? 'active text-accent' : '' }}">À PROPOS</a>
            
            <div class="mt-8 sm:mt-10">
                <a href="{{ path('app_contact') }}" class="premium-btn !text-lg sm:!text-xl !px-10 !py-4 nav-link {{ current_route == 'app_contact' ? 'active' : '' }}">ME CONTACTER</a>
            </div>
        </div>

        {# Background Glow for Mobile Menu #}
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full h-full -z-10 opacity-20 pointer-events-none">
            <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-accent blur-[180px] rounded-full"></div>
            <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-blue-900 blur-[180px] rounded-full"></div>
        </div>
    </div>
</nav>
